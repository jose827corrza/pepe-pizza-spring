spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pepe_pizza_db
    username: root
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.PostgreSQLDialect
      jdbc:
        lob:
          non-contextual-creation:true:
    properties:
      hibernate:
        show_sql:true

